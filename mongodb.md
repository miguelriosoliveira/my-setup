# Export data from one table to a JSON file (remote db)
`mongoexport --host <ip:port> --db <db name> --collection <table name> --out <file.json> --jsonArray`

Source(s):
- https://docs.mongodb.com/manual/reference/program/mongoexport/
